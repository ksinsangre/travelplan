<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nelsito.travelplan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".trips.map.MapTripsActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".trips.edit.EditTripActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".user.AnonymousActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".user.WaitForVerificationActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".user.ProfileActivity"
            android:screenOrientation="portrait" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/api_key" />

        <activity
            android:name=".trips.detail.TripDetailActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".trips.add.AddTripActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".trips.list.TripsListActivity"
            android:screenOrientation="portrait" /> <!-- Your Splash Activity theme -->
        <activity
            android:name=".SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>